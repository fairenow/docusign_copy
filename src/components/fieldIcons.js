import { Pen, Hash, Type, Calendar, CheckSquare, PencilLine } from 'lucide-react'

/** Icon per field type (see FIELD_TYPES in lib/fields.js). */
export const FIELD_ICONS = { signature: Pen, initials: Hash, text: Type, date: Calendar, checkbox: CheckSquare, prefill: PencilLine }
