# Supabase project (Project Settings → API). The publishable key is safe to expose in the browser;
# never put the service-role key in a VITE_ variable.
VITE_SUPABASE_URL=https://tdgwdniwwkqqyfuoxnwx.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=

# Sign-in is limited to this email domain (the database enforces it too)
VITE_ALLOWED_EMAIL_DOMAIN=flmlnk.com
